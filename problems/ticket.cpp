#include <vector>
#include <iostream>
#include <stdexcept>
#include "core/judge.h"

using std::vector;
using std::runtime_error;

/** 
 *  蒟蒻hzwer NOIP2014惨跪，他依稀记得他的准考证号是37，现在hzwer又将要面临一场比赛，他希望准考证号不出现37（连续），
 *  同时他又十分讨厌4，所以也不希望4出现在准考证号中。。。现在他想知道在A和B之间有多少合法的准考证号
 *
 *  参数 A: 如题
 *  参数 B: 如题
 *  输出： 一个数字，为合法准考证号的个数
 *
 *  数据规模：
 *  20%的数据满足 1 <= A <= B <= 1000000
 *  100%的数据满足 1 <= A <= B <= 2000000000
 *  
 */
static void solution (std::ostream &out, int A, int B) {
	throw runtime_error("Not implemented");
}

DECLARE_PROBLEM(ticket, solution);
